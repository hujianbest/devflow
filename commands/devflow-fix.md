---
description: DevFlow 缺陷修复——在标准 change 工件中先复现和根因，再判断 delta 并以 TDD 最小修复
---

执行 DevFlow 缺陷修复。

1. 读取 `skills/using-devflow/SKILL.md`，创建或读取 change 并通过 baseline preflight。
2. 读取 `skills/devflow-fix/SKILL.md` 及其直接 references。
3. 完整执行复现、根因、delta/N/A 判断、TDD 和全部评审收尾门禁；缺陷协议以该 SKILL 为准。
