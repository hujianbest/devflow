---
description: 为仓库建立覆盖全仓真实领域的首版领域 wiki，只落盘 wiki 结果
---

初始化领域 wiki。

1. 读取 `domain-knowledge-library/domain-knowledge-init/SKILL.md` 及其直接 references。
2. 按发现协议与骨架协议做有界盘点并编译完整栏目；已存在且非空的 wiki 不得静默覆盖。
3. 写完 `status=complete`。不要创建 `reviews/` 或其它评审文件。不要把构建产物或导出文档树当源或模板。
