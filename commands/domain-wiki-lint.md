---
description: 巡检领域 wiki 的结构健康，不写评审报告
---

巡检领域 wiki。

1. 读取 `domain-wiki-skills/domain-wiki-lint/SKILL.md`。
2. 先跑机械校验并报告断链、孤儿、stub、坏 mermaid；按磁盘重写栏目 index。破坏性改动先问人。
3. 不要创建 `reviews/`，不要改 `.last-update.json` 的 `status`。
