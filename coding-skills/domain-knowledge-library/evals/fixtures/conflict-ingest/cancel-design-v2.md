# 订单取消 V2 设计

状态：已通过技术评审，尚未实现。

目标：未来允许已经进入 `SHIPPED` 的订单在仓库未出库时发起取消。

设计要点：

1. `SHIPPED` 订单先向仓储系统申请拦截。
2. 仓储确认拦截成功后，订单转为 `CANCELLED`。
3. 拦截失败时保持 `SHIPPED`。
4. 本设计不代表当前生产行为。

该变化需要订单领域 Owner 确认业务规则，并需要仓储团队确认跨系统契约。
